name: Writerside Documentation

on:
  push:
    branches:
      - main

jobs:
  ci-documentation:
    uses: ./.github/templates/ci.documentation.yml
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
