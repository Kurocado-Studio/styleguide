name: Writerside Documentation

on:
  push:
    branches:
      - main

jobs:
  ci-documentation:
    uses: ./.github/templates/ci.documentation.yml@v1
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
